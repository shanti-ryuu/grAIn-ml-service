services:
  - type: web
    name: grain-ml-service
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "5000"
      - key: FLASK_DEBUG
        value: "false"
